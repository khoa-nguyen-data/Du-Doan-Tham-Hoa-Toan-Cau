# 1.Tạo virtual environment:
# py -3 -m venv .venv

# 2.Kích hoạt
# Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
# .\.venv\Scripts\Activate.ps1

# 3.Xem chi tiết chính sách thực thi 
# Get-ExecutionPolicy -List


#-----------------------------------------------------------------------------

# LƯU Ý : CHỈ CẦN THỰC HIỆN 3 DÒNG ĐẦU, 
# NẾU CÒN BỊ CHẶN MỚI THỰC HIỆN TIẾP DÒNG DƯỚI
# Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Bypass

#-----------------------------------------------------------------------------




# 4.Cài đặt các package cần thiết
# python -m pip install --upgrade pip
# python -m pip install pandas numpy matplotlib seaborn plotly folium notebook ipykernel


# 5.Đăng ký kernel cho Jupyter Notebook
# python -m ipykernel install --user --name=du-doan-venv
# --display-name="Python (du-doan-venv)"


# 6.Sử dụng kernel trong Jupyter Notebook

# 7.Hủy kích hoạt venv
# deactivate

# 8.Xoá venv (nếu cần)
# Remove-Item -Recurse -Force .\.venv
# End of setup_venv_hotro
# Lưu ý: Chạy file setup_venv.ps1 để tự động hoá các bước trên
# setup_venv.ps1
# Tham số:
#   $venvName: Tên thư mục virtual environment (mặc định: .venv)
#   $kernelName: Tên kernel Jupyter (mặc định: du-doan-venv)
#   $displayName: Tên hiển thị kernel trong Jupyter (mặc định: Python (du-doan-venv))
# Sử dụng:
#   .\setup_venv.ps1 -venvName ".venv" -kernelName "du-doan-venv" -displayName "Python (du-doan-venv)"
# Hoặc chỉ cần chạy:
#   .\setup_venv.ps1
# để sử dụng các giá trị mặc định.
# Nội dung file setup_venv.ps1:
# setup_venv.ps1 CÓ FILE BÊN NGOÀI .
